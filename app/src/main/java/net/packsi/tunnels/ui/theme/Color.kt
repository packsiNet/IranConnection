package net.packsi.tunnels.ui.theme

import androidx.compose.ui.graphics.Color

// Brand palette extracted from the SafeTunnels design (dc.html sources)
object AppColors {
    val ScreenBg = Color(0xFFF1F3F7)
    val CardBg = Color(0xFFFFFFFF)
    val Divider = Color(0xFFF0F2F7)
    val NavBorder = Color(0xFFEAEDF4)

    val TextPrimary = Color(0xFF18182A)
    val TextMuted = Color(0xFF94A0B4)
    val TextFaint = Color(0xFFB0BAC8)
    val Chevron = Color(0xFFCDD3DE)

    // Teal (connected / active)
    val Teal = Color(0xFF4BBDB8)
    val TealLight = Color(0xFF68D0CA)
    val TealDark = Color(0xFF3DA8A3)

    // Red (disconnected)
    val Red = Color(0xFFEF4444)
    val RedLight = Color(0xFFFF7575)
    val RedPink = Color(0xFFE73C6E)

    // Misc
    val Gold = Color(0xFFF5A623)
    val GoldStroke = Color(0xFFE4950F)
    val MapDot = Color(0xFFB8C2CF)
    val SideButton = Color(0xFFC0C8D4)
    val ToggleOff = Color(0xFFD0D8E4)
    val HomeIndicator = Color(0xFFC8D0DC)
}
